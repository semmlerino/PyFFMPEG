# ⚠️  DEPRECATED: This file is kept for backwards compatibility only.
# The project now uses uv with pyproject.toml for dependency management.
# To install dependencies, run: uv sync
# See pyproject.toml for the authoritative dependency list.

PySide6>=6.0.0
psutil>=5.9.0
pillow>=10.0.0
Jinja2>=3.0.0
OpenEXR>=1.3.0
olefile>=0.46
typing_extensions>=4.0.0  # For Python 3.11 compatibility (@override decorator)