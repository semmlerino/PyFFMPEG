[tool.ruff]
# Same as Black
line-length = 88
indent-width = 4

# Python version (matches actual requirement: Python 3.11+)
target-version = "py311"

# Exclude common directories
exclude = [
    ".git",
    ".ruff_cache",
    "__pycache__",
    "venv",
    ".venv",
    "venv_py311",
    "test_venv",
    "archived",
    "copy",
]

[tool.ruff.lint]
# Enable comprehensive rule set including type-checking
select = [
    "E", "F",  # pycodestyle, Pyflakes
    "I",       # isort
    "UP",      # pyupgrade - modern type syntax
    "TCH",     # flake8-type-checking - TYPE_CHECKING imports
    "ANN",     # flake8-annotations - missing annotations
]

# Never enforce line length violations
# ANN401 - Allow Any in *args and **kwargs (common pattern)
# TC003 - Allow standard library imports outside type-checking (Path is used at runtime)
ignore = ["E501", "ANN401", "TC003"]

# Allow autofix for all enabled rules
fixable = ["ALL"]

[tool.ruff.lint.flake8-type-checking]
runtime-evaluated-base-classes = ["PySide6.QtCore.QObject"]

[tool.ruff.format]
# Use single quotes for strings
quote-style = "double"

# Indent with spaces
indent-style = "space"

# Respect magic trailing commas
skip-magic-trailing-comma = false

# Format docstrings
docstring-code-format = true

[tool.basedpyright]
# Use recommended mode for better type safety with basedpyright improvements
typeCheckingMode = "recommended"

# Python version (matches minimum requirement: Python 3.11+)
pythonVersion = "3.11"

# Virtual environment
venvPath = "."
venv = "venv"

# Files to include
include = [
    "shot_model.py",
    "cache_manager.py", 
    "utils.py",
    "raw_plate_finder.py",
    "threede_scene_finder.py",
    "threede_scene_model.py",
    "main_window.py",
    "config.py"
]

# Files to exclude
exclude = [
    "venv",
    "**/__pycache__",
    "build",
    "dist"
]

# Stub path
stubPath = "."

# Error reporting levels
reportMissingImports = "warning"
reportMissingTypeStubs = "warning"
reportUnnecessaryTypeIgnoreComment = "error"
reportIgnoreCommentWithoutRule = "error"
reportPrivateUsage = "warning"
# Stricter Unknown type reporting for better type safety
reportUnknownMemberType = "error"
reportUnknownArgumentType = "error"
reportUnknownLambdaType = "warning"
reportUnknownVariableType = "error"
reportUntypedFunctionDecorator = "warning"
reportUntypedClassDecorator = "warning"
reportUntypedBaseClass = "error"
reportUntypedNamedTuple = "error"
reportCallInDefaultInitializer = "warning"
reportImplicitStringConcatenation = "warning"
reportMissingSuperCall = "warning"
reportPropertyTypeMismatch = "error"
reportFunctionMemberAccess = "error"
reportIncompatibleMethodOverride = "error"
reportIncompatibleVariableOverride = "error"
reportOverlappingOverload = "error"